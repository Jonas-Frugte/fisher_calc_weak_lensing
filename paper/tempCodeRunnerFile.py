
    # cov_matrices = [
    #     planck_reduced,
    #     inv(inv(planck_reduced) + visp_f_reduced),
    #     #planck_reduced + inv(visb_f_reduced),
    #     inv(inv(planck_reduced) + visb_f_reduced + visp_f_reduced)
    # ]

    # labels = ['planck only', 'powerspectra', 'power- $+$ bispectra']
